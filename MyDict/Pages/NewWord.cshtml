@page
@model MyDict.Pages.NewWordModel
@{
}

<form method="post">
    <label class="form-label">Search</label>
    <input type="text" class="form-control" name="searchField">
    <div id="passwordInfo" class="form-text">
        The word from your database will be returned if exists. Else, you will be able to create one.
    </div>
    <div class="form-group ">
        <input type="submit" class="btn btn-primary" />
    </div>
</form>