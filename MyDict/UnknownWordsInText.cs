using MyDict.Model;

namespace MyDict
{
    public class UnknownWordsInText
    {
        private static readonly AppDbContext _context = new();
        private readonly List<string> _unknownWords = new();

        private void GetUnknownWords()
        {
            

        }
    }
}
